# KPost UI — Automated Bug Report

> Generated by the KPost UI Test Bench on 2026-08-14 09:48:11 UTC.
> **This file is generated — do not edit by hand.** Re-run `npm test` to regenerate.

## Executive Summary

| Metric | Value |
| --- | --- |
| Report generated | 2026-08-14 09:48:11 UTC |
| Environment | Local |
| Target | `https://192.168.0.158:3000` |
| Browser projects | chromium, firefox, webkit, mobile-chrome |
| Tests planned | 236 |
| Reached a result | 236 |
| Passed / Failed / Skipped | 45 / 185 / 6 |
| Pass rate (of tests that ran) | 19.6% |
| Run duration | 673.2s |
| Playwright verdict | failed |

### Verdict

**2 high-severity application defect(s) open — fix before the next release.**

### Defect counts by severity

| Severity | Count |
| --- | --- |
| High | 2 |
| Medium | 4 |
| Low | 1 |
| **Total distinct defects** | **7** |

### Defects by module

| Module | Defects |
| --- | --- |
| KMail | 2 |
| Auth | 1 |
| Katchup | 1 |
| KEcommerce | 1 |
| KNews | 1 |
| KPay | 1 |

### Defect index

| ID | Severity | Module | Title |
| --- | --- | --- | --- |
| KPOST-AUTH-001 | High | Auth | After logout, protected routes are not redirected back to /login. |
| KPOST-KMAIL-002 | High | KMail | POST /v2/sentMail/postMail/ intermittently returns 401 for a valid session. |
| KPOST-KATCHUP-001 | Medium | Katchup | Katchup does not handle a failing contacts backend; it raises instead of degrading. |
| KPOST-KECOM-001 | Medium | KEcommerce | The KEcommerce catalog intermittently renders as an empty pane. |
| KPOST-KMAIL-001 | Medium | KMail | KMail raises an uncaught TypeError on load (UnopenedMailAsync). |
| KPOST-KNEWS-001 | Medium | KNews | KNews renders an empty feed with no error state when its news bridges fail. |
| KPOST-KPAY-001 | Low | KPay | The icon rail shows a KPay entry that silently does nothing. |

---

## Defect detail

### KPOST-AUTH-001 — After logout, protected routes are not redirected back to /login.

- **Severity:** High
- **Module:** Auth

**Evidence**

Logout correctly clears accessToken, refreshToken, Authuser and deviceIdentity_primary and lands on /login. But navigating back to /home afterwards stays on /home and renders a dead, shell-less page (the Quick Access button never appears) instead of redirecting to the login screen.

**Expected**

Visiting a protected route while signed out redirects to /login.

**Observed in this run**

Observed by 1 test(s) in this failed run: after logout, protected routes redirect back to login [chromium] (failed)

---

### KPOST-KMAIL-002 — POST /v2/sentMail/postMail/ intermittently returns 401 for a valid session.

- **Severity:** High
- **Module:** KMail

**Evidence**

The same self-send, from a freshly logged-in session with an unexpired token (24h JWT lifetime), is sometimes answered 400 "Duplicate IDs are present in ToAddress, CopyList, or ConfidentialCopyList" (the expected validation) and sometimes 401 — observed switching between the two across consecutive runs on 2026-08-12. A backend that authenticates a session for one request and rejects it for the next is failing auth intermittently; the historical KMail 401 force-logout (now fixed) was the same category of fault.

**Expected**

A valid session is authenticated consistently; postMail never 401s it.

**Observed in this run**

Observed by 1 test(s) in this failed run: sending a mail to yourself is rejected with the documented error [chromium] (failed)

---

### KPOST-KATCHUP-001 — Katchup does not handle a failing contacts backend; it raises instead of degrading.

- **Severity:** Medium
- **Module:** Katchup

**Evidence**

GET localhost:8989/v2/contacts/getImportedPhoneContacts/ and POST localhost:8989/v2/contacts/myUnknownKatchupContacts/ intermittently return 500. The app then raises an unhandled "[object Object]" at handleError rather than degrading, and the conversation/search surface never renders. This is why these tests pass in isolation but fail when the backend happens to error.

**Expected**

A failing contacts call degrades gracefully — the pane still renders and search still reports its result state.

**Observed in this run**

Observed by 2 test(s) in this failed run: searching a unique term updates the feed and clears again [chromium] (failed); the Recents and Contacts tabs both render the conversation feed [chromium] (failed)

---

### KPOST-KECOM-001 — The KEcommerce catalog intermittently renders as an empty pane.

- **Severity:** Medium
- **Module:** KEcommerce

**Evidence**

During a full serial suite run on 2026-08-12, /e-commerce rendered only the app-shell header with zero merchant tiles (the page snapshot shows an empty content area), failing both catalog tests; the same tests pass 5/5 when the spec runs in isolation, before and after. The module shows no error state when this happens — the catalog is simply missing. Root cause not yet captured (no failed catalog request has been observed; the tile source may be a data call that fails silently or an app-side race under longer sessions).

**Expected**

The catalog renders its merchant tiles on every load, or shows an error state.

**Observed in this run**

Observed by 2 test(s) in this failed run: the merchant catalog renders [chromium] (failed); each sampled merchant tile is visible [chromium] (failed)

---

### KPOST-KMAIL-001 — KMail raises an uncaught TypeError on load (UnopenedMailAsync).

- **Severity:** Medium
- **Module:** KMail

**Evidence**

Uncaught "TypeError: Cannot read properties of undefined (reading 'status')" at UnopenedMailAsync, thrown as soon as /kmail loads. Verified 2026-08-12: the module underneath still functions — all three tabs work once the dev-only overlay is dismissed — so the suite dismisses the overlay (recording it as a "dismissed-app-error" annotation) and verifies the real behaviour. The error itself remains unfixed: in a production build it would surface as the unopened-mail feature failing silently.

**Expected**

KMail loads without raising any uncaught error.

**Observed in this run**

Observed by 1 test(s) in this failed run: the mailbox exposes Recents, Contacts and Status of Mails tabs [chromium] (passed)

---

### KPOST-KNEWS-001 — KNews renders an empty feed with no error state when its news bridges fail.

- **Severity:** Medium
- **Module:** KNews

**Evidence**

KNews fetches content through public RSS bridges (corsproxy.io, rss2json.com) that intermittently answer 503/422/429 — rss2json rate-limiting (429) was captured directly. When that happens the main feed renders zero cards and no error or empty-state message (page snapshot: a main landmark with no links), minutes after the same feed rendered normally. A news feed with nothing in it and no explanation is indistinguishable from a broken app to the user.

**Expected**

When the news sources fail, the feed shows an error/retry state instead of silently rendering nothing.

**Observed in this run**

Observed by 2 test(s) in this failed run: the news feed renders cards [chromium] (failed); news cards support click-through (real links with targets) [chromium] (failed)

---

### KPOST-KPAY-001 — The icon rail shows a KPay entry that silently does nothing.

- **Severity:** Low
- **Module:** KPay

**Evidence**

The rail renders div.icon-KP_12-KWallet and its expanded labels include "KPay", but clicking either leaves the URL unchanged, Quick Access does not offer the module, and /kpay, /kwallet, and /pay all render the 404 page (verified 2026-08-13). A visible nav entry that silently does nothing is broken UX from the user’s side, whatever the roadmap says — it should be hidden, disabled with an affordance, or wired up.

**Expected**

Rail entries either navigate somewhere or are visibly disabled/absent.

**Observed in this run**

Observed by 3 test(s) in this failed run: the rail advertises KPay but the launcher does not offer it [chromium] (passed); clicking the KPay rail entry navigates nowhere [chromium] (passed); the KPay route is not implemented and 404s [chromium] (passed)

---

_These are **application** defects confirmed against the live app, registered in
`src/utils/known-defects.ts`. The registry documents a defect; it never suppresses
one — no assertion is weakened to accommodate an entry here. A test that observes a
defect usually FAILS, because it asserts the behaviour the app should have; a few
PASS because they pin the broken behaviour as a contract and are designed to turn red
the day the app changes. "Observed in this run" above records which tests saw each
defect and with what outcome. Full traces, screenshots and video: `npm run report`._
